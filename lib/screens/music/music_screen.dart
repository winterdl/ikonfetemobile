import 'package:flutter/material.dart';
import 'package:ikonfete/zoom_scaffold/zoom_scaffold.dart';

Screen musicScreen(String uid) {
  return Screen(
    title: "Music",
    contentBuilder: (ctx) => Container(
          color: Colors.white,
        ),
  );
}
